<!DOCTYPE html>
<html lang="mk">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>НАСЛОВ НА БЛОГОТ – Оаза Медика</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Lato:wght@300;400;600&display=swap" rel="stylesheet" />
  <style>
    :root {
      --teal:#2a7a7a; --teal-dark:#1d5e5e; --teal-light:#e4f2f2;
      --green:#1a5c2a; --green-light:#e8f5ec;
      --cream:#f9faf8; --white:#ffffff;
      --text:#1c2a22; --text-mid:#3d5046; --text-light:#6b8070;
    }
    *, *::before, *::after { box-sizing:border-box; margin:0; padding:0; }
    html { scroll-behavior:smooth; }
    body { font-family:'Lato',sans-serif; background:var(--cream); color:var(--text); line-height:1.8; }

    /* NAV */
    nav {
      background:rgba(255,255,255,0.97);
      backdrop-filter:blur(10px);
      border-bottom:1px solid rgba(42,122,122,.12);
      padding:0 5%;
      display:flex;
      align-items:center;
      justify-content:space-between;
      height:64px;
      position:sticky;
      top:0;
      z-index:100;
    }
    .nav-home { text-decoration:none; color:var(--teal); font-weight:600; font-size:.88rem; display:flex; align-items:center; gap:6px; }
    .nav-home:hover { color:var(--teal-dark); }
    .nav-blog { text-decoration:none; color:var(--text-light); font-size:.82rem; }
    .nav-blog:hover { color:var(--teal); }

    /* LAYOUT */
    .container { max-width:760px; margin:0 auto; padding:3rem 5% 5rem; }

    /* HEADER */
    .blog-label {
      font-size:.75rem; font-weight:700; letter-spacing:.12em;
      text-transform:uppercase; color:var(--teal); margin-bottom:.6rem;
    }
    h1 {
      font-family:'Playfair Display',serif;
      font-size:clamp(1.9rem,4vw,2.8rem);
      color:var(--text);
      margin-bottom:1rem;
      line-height:1.2;
    }
    .meta {
      display:flex; gap:1rem; flex-wrap:wrap;
      font-size:.82rem; color:var(--text-light);
      margin-bottom:2.5rem;
      padding-bottom:2rem;
      border-bottom:1px solid rgba(42,122,122,.1);
    }

    /* CONTENT */
    .content p { margin-bottom:1.3rem; color:var(--text-mid); font-weight:300; font-size:1rem; }
    .content h2 {
      font-family:'Playfair Display',serif;
      font-size:1.5rem; color:var(--text);
      margin:2.5rem 0 .8rem;
    }
    .content h3 {
      font-family:'Playfair Display',serif;
      font-size:1.1rem; color:var(--teal);
      margin:1.8rem 0 .5rem;
    }
    .content ul, .content ol {
      margin:.5rem 0 1.3rem 1.5rem;
      color:var(--text-mid); font-weight:300;
    }
    .content li { margin-bottom:.4rem; }
    .content strong { color:var(--text); font-weight:600; }

    /* TIP BOX */
    .tip-box {
      background:var(--green-light);
      border-left:4px solid var(--green);
      border-radius:0 12px 12px 0;
      padding:1.2rem 1.5rem;
      margin:2rem 0;
      font-size:.93rem;
      color:var(--green);
      line-height:1.7;
    }
    .tip-box strong { color:var(--green); }

    /* CTA STRIP */
    .cta-strip {
      background:linear-gradient(135deg,var(--teal),var(--green));
      border-radius:16px;
      padding:2rem 2.5rem;
      text-align:center;
      margin-top:3rem;
    }
    .cta-strip p { color:rgba(255,255,255,.85); font-weight:300; margin-bottom:1rem; }
    .cta-strip a {
      display:inline-flex; align-items:center; gap:8px;
      background:var(--white); color:var(--teal);
      padding:.7rem 1.6rem; border-radius:50px;
      font-weight:700; font-size:.9rem; text-decoration:none;
      transition:transform .2s;
    }
    .cta-strip a:hover { transform:translateY(-2px); }

    /* BACK */
    .back-link {
      display:inline-flex; align-items:center; gap:6px;
      margin-top:3rem; color:var(--teal);
      text-decoration:none; font-weight:600; font-size:.88rem;
      border:2px solid var(--teal); border-radius:50px;
      padding:.5rem 1.2rem; transition:all .2s;
    }
    .back-link:hover { background:var(--teal); color:var(--white); }

    @media(max-width:600px) {
      .container { padding:2rem 4% 4rem; }
    }
  </style>
</head>
<body>

<nav>
  <a href="../index.html" class="nav-home">← Оаза Медика</a>
  <a href="../index.html#blog" class="nav-blog">Едукативен блог</a>
</nav>

<div class="container">

  <!-- ══ ПРОМЕНИ ГИ ОВИЕ ══ -->
  <p class="blog-label">КАТЕГОРИЈА</p><!-- пр. Кардиологија, Дијабетес, Превенција... -->
  <h1>Наслов на блог написот</h1>
  <div class="meta">
    <span>📅 Месец Година</span><!-- пр. Мај 2025 -->
    <span>✍️ Д-р Гордана Стевчева</span>
  </div>
  <!-- ══════════════════════ -->

  <div class="content">

    <p>Вовед — напишете го главниот контекст на темата и зошто е важна за пациентите.</p>

    <h2>Прва тема / поднаслов</h2>
    <p>Текст за оваа тема...</p>

    <h2>Втора тема / поднаслов</h2>
    <p>Текст за втората тема...</p>
    <ul>
      <li>Прва точка</li>
      <li>Втора точка</li>
      <li>Трета точка</li>
    </ul>

    <!-- Зелена кутија за важни совети -->
    <div class="tip-box">
      💡 <strong>Важно:</strong> Напишете некој клучен совет или предупредување тука.
    </div>

    <h2>Трета тема / поднаслов</h2>
    <p>Текст за третата тема...</p>

    <p>Заклучок — кратко резиме и охрабрување да побараат совет од лекар.</p>

  </div>

  <!-- Повик за акција — оставете го, работи автоматски -->
  <div class="cta-strip">
    <p>Имате прашање или сакате да закажете преглед?</p>
    <a href="../index.html#contact">📞 Контактирајте не</a>
  </div>

  <a class="back-link" href="../index.html#blog">← Назад кон блогот</a>

</div>

</body>
</html>
